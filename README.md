# Apple-Inspo

Apple-inspired dashboard UI with glassmorphism, soft shadows, rounded corners, and spacious layout.

## Preview

Open `index.html` directly in a browser, or serve the folder with any static server.

## Project Files

- `index.html` — dashboard structure
- `styles.css` — glassmorphism styling and layout
- `main.js` — lightweight clock/date interactivity